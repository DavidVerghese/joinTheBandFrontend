function SocialMedia(props) {
  console.log(props.display);
  return (
    <div>
      <button onClick={console.log("click!")}>Social Media/Contact Info</button>
    </div>
  );
}

export default SocialMedia;
